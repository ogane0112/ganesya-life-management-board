{
  "name": "@ganesya/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker API proxy: fetches the private life-management repo via GitHub API and returns aggregated RPG status JSON",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ganesya/stats-engine": "0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "wrangler": "^3.84.0"
  }
}
